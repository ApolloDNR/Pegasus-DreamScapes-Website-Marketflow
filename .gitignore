# Dependencies & build output
node_modules
dist
server/public
*.tar.gz

# OS / editor
.DS_Store
*.swp
*.swo

# Vite local
vite.config.ts.*

# Replit / local tooling artifacts (must not be committed)
.cache/
.upm/
.config/
.local/
.agents/
# .claude is local tooling EXCEPT the shared project skills, which every
# Claude surface (Cowork cloud + Claude Code local) must load identically.
.claude/*
!.claude/skills/
.claude-config/
.claude-plugin/
.replit-cache/
.history/
.vscode/
.idea/

# Local credentials & env (use .env.example only)
.env
.env.*
!.env.example

# Backup / scratch
*.bak
*.old
*.orig
*.backup
__backup__/
__scratch__/

# Replit pasted transcripts (chat artifacts, never website assets)
attached_assets/Pasted-*.txt
attached_assets/Pasted-*.md

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# screenshot/QA build output (regenerated on demand)
client/dist-hero/
client/dist-qa/
