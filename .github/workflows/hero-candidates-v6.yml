name: Hero candidates v6 (retired)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "retired — hero is now type-led editorial (no images)"
