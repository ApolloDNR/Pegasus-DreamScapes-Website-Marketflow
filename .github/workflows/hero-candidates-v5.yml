name: Hero candidates v5 (retired)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "retired — hero managed by hero-candidates-v6"
