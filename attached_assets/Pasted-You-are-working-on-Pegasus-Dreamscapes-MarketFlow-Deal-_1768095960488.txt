You are working on Pegasus Dreamscapes MarketFlow (Deal Discovery + Offer + Negotiation). The current issue: forms and actions are inconsistent across views (Grid/Swipe/Deal Details). “Make Offer / Counter / Accept” sometimes open different forms depending on entry point. This must be unified.

GOAL
1) MarketFlow has 3 lanes (tabs): 
   A) Wholesale Assignments
   B) Developments / Capital Raises
   C) Listings (ready-to-move-in / on-market/off-market)
2) Each lane has its OWN purpose-specific form(s).
3) Regardless of entry point (Grid cards, Swipe view, Deal Details page, buttons, deep links), the SAME action must open the SAME form, backed by the SAME data model.
4) Offer Studio is for Capital Raises (advanced) — NOT for simple Wholesale assignment offers.
5) Wholesale negotiation is simpler (light form) but still routes to a Negotiation Room once a counter/offer exists.
6) Negotiation Room acts like escrow: opens when there are 2 principals in negotiation. It contains columns for (a) offer history ladder/logs (b) chat (c) Peggy AI advisor panel.

NON-NEGOTIABLE: CANONICAL FORM ROUTING
Implement a single canonical “action router” so the UI never directly opens random modals per view.
- Create one function: openDealAction(dealId, lane, action, context)
  - lane: "WHOLESALE" | "CAPITAL" | "LISTING"
  - action: "VIEW" | "ACCEPT" | "COUNTER" | "MAKE_OFFER" | "REQUEST_INFO" | "SCHEDULE_SHOWING" | "NEGOTIATION_ROOM"
  - context: { source: "grid" | "swipe" | "detail", dealType, userRole }
All buttons everywhere call this same function.
This function decides which modal/page opens.

DATA MODEL / TYPES (UNIFY)
Make sure every deal has:
- id
- lane (WHOLESALE/CAPITAL/LISTING)
- dealType (assignment, JV, debt raise, equity raise, hybrid, listing_on_market, listing_off_market)
- posterId (operator/wholesaler/agent)
- status (available, negotiable, under_contract, funded, etc.)
- terms (poster terms object)
- negotiationId (nullable; created when negotiation begins)

Also create Offer objects:
- id
- dealId
- negotiationId
- createdBy
- offerKind ("WHOLESALE_ASSIGNMENT" | "WHOLESALE_JV" | "CAPITAL_INVESTMENT" | "LISTING_INQUIRY")
- payload (JSON)
- status (sent, countered, accepted, rejected)
- timestamps

Create Negotiation objects:
- id
- dealId
- participants (poster + counterparty)
- offers[] (or query offers by negotiationId)
- createdAt

LANE BEHAVIOR SPEC
====================================

A) WHOLESALE ASSIGNMENTS (simple negotiation)
Cards show: address, ask, ARV, repairs, assignment fee, spread/profit, negotiable badge.
Primary actions (on card, swipe, detail must match):
- View Deal -> Deal Details page for wholesale
- Accept Terms -> Accept Wholesale Terms modal (simple confirm)
- Counter Offer -> Counter Wholesale Offer modal (simple form)
- Optional: Request JV Partnership -> JV Request modal (if deal supports JV)

IMPORTANT: The “Counter Offer” and “Make Offer” must be the same canonical wholesale form. The Deal Details page currently has a different Make Offer flow—REMOVE divergence.
All wholesale entry points must open the same component(s).

Wholesale forms:
1) WholesaleAcceptModal (simple)
   - shows poster’s asking assignment fee + key terms (EMD, closing date target, inspection period)
   - user clicks Confirm Accept
   - system creates Offer (offerKind WHOLESALE_ASSIGNMENT) with payload = poster terms
   - system creates negotiation if not exists
   - route user to Negotiation Room after accept OR show toast “Accepted – negotiation opened”

2) WholesaleCounterModal (simple, single page modal)
   Fields:
   - Assignment Fee Offer (required)
   - Earnest Money Deposit (required)
   - Closing Date (required)
   - Inspection Period (days) (required)
   - Message / Notes (optional)
   On submit:
   - create offer + create negotiation if needed
   - route to Negotiation Room
   NOTE: This same modal is used whether user clicks Counter from grid, swipe, or “Make Offer” inside Deal Details.

3) WholesaleJVRequestModal (optional, if JV)
   Fields:
   - Role Selection (capital partner / operator / buyer-bringer / other)
   - Proposed Split slider (10–90) OR fixed options
   - Contributions checklist (what user brings)
   - Message
   Submit creates offerKind WHOLESALE_JV + negotiation (optional depending on product choice; if negotiation created, route to Negotiation Room)

B) DEVELOPMENTS / CAPITAL RAISES (advanced negotiation)
Cards show: project name, location, funding goal, remaining, structure (Debt/Equity/Hybrid), progress bar, min investment, target return/split.
Primary actions everywhere must match:
- View Details -> Capital Project details page
- Accept Terms -> Accept Investment Terms (simple confirm OR fast-path inside Offer Studio)
- Counter Offer -> Offer Studio (advanced multi-step)
- Make Offer -> SAME as Counter Offer (Offer Studio)
No inconsistent forms.

Capital flow:
- If action is ACCEPT: open CapitalAcceptModal
  - confirm investing at poster terms (investment amount defaults to min investment or previously entered)
  - for debt: amount + term length (if fixed) + confirm
  - for equity: amount + confirm split (if fixed)
  - on submit => create offerKind CAPITAL_INVESTMENT + negotiation + route to Negotiation Room
- If action is COUNTER or MAKE_OFFER: open Offer Studio
  Offer Studio must start with a first screen:
    “Accept Terms” OR “Counter / Customize”
    - Accept Terms button: immediate create offer at poster terms -> negotiation room
    - Counter button: proceed to advanced structuring steps

Offer Studio steps (only for CAPITAL lane):
1) Deal Structure (Debt / Equity / Hybrid)
2) Terms & Price:
   - Investment amount (required)
   - Expected return % (for debt) OR Profit split % (for equity) OR both (hybrid)
   - Term length months (required)
3) Financing / Protections:
   - Payment schedule / distribution frequency (optional if you have it)
   - Investor protections checklist (optional: lien, preferred return, reporting)
4) Review & Submit (shows summary + creates offerKind CAPITAL_INVESTMENT)
After submit -> route to Negotiation Room.

C) LISTINGS (simple inquiry)
Cards show: address, beds/baths/sqft, days on market, price, tags:
- On Market / Off Market
- Move-In Ready / Needs Renovation / Minor Updates
Primary actions:
- View Listing -> Listing details page
- Request Info -> ListingInquiryModal
- Schedule Showing -> ShowingRequestModal
No Offer Studio here.

ListingInquiryModal fields:
- Full name (prefill if logged in)
- Phone
- Email
- Buyer type: (cash / financed / investor / first time)
- Pre-approval? (yes/no)
- Message (what info they want)
Submit creates offerKind LISTING_INQUIRY (or Inquiry object) and sends message to agent/operator.

ShowingRequestModal fields:
- Preferred dates/times (2–3 options)
- Are you working with an agent? (yes/no)
- Financing type
- Message
Submit creates showing request.

CONSISTENCY REQUIREMENTS (fix what’s broken now)
1) Grid and Swipe must use the exact same components/actions as Deal Details.
2) “Counter Offer” modal title/fields must match the “Make Offer” entry point for the same lane.
3) Remove duplicate forms: there should not be one “Counter modal” from discovery and a different “Make offer” from detail.
4) If some sections are not fully showing, audit conditional rendering:
   - ensure all fields render for each lane’s forms
   - ensure modals have responsive height and internal scroll
   - ensure required field validation doesn’t hide sections

NEGOTIATION ROOM (Escrow-like)
When a negotiation exists, user can enter negotiation room.
Layout: 3-column desktop, stacked mobile.
Left column: Offer Ladder / History (offers timeline with status, amounts, terms, timestamps; accept/reject/quick counter buttons)
Middle column: Chat between poster + counterparty (messages)
Right column: Peggy AI panel (insights + quick actions)
Also provide a “Terms” tab or “Deal Summary” panel.
Negotiation room must work for WHOLESALE and CAPITAL negotiations.

Routing:
- After any offer submit/accept => route to /marketflow/negotiation/:negotiationId
- “Go to Negotiation Room” button available on deal details if negotiation exists.

IMPLEMENTATION TASKS (do these in order)
1) Identify all current buttons that open forms (grid card, swipe card, view deal page, deal detail actions, negotiation room quick counter).
2) Replace their onClick with openDealAction(...)
3) Create lane-based canonical modal components:
   - WholesaleAcceptModal
   - WholesaleCounterModal
   - WholesaleJVRequestModal
   - CapitalAcceptModal
   - OfferStudio (capital only)
   - ListingInquiryModal
   - ShowingRequestModal
4) Ensure data saved matches unified Offer schema and creates/links Negotiation.
5) Ensure “Listings” tab exists and is fully wired with its actions (currently missing before; now must be present with forms).
6) Add Beta banner support: MarketFlow is Beta (optional but recommended) and ensure front site remains stable.

QUESTIONS YOU ASKED ME — HERE ARE THE ANSWERS (DO NOT ASK AGAIN)
- Counter-offers should use an Offer Ladder / history view inside the Negotiation Room (not only simple forms).
- Listings lane needs View Listing, Request Info, Schedule Showing with the two inquiry forms described.
- Match score badges are optional; not required for capital and listings right now.
- Saved/bookmarked should work across all lanes, but do not block the canonical forms fix.

DELIVERABLE
Commit code that:
- Makes every offer/counter/accept action consistent across grid/swipe/details
- Keeps Offer Studio only for Capital Raises
- Adds Listings lane + inquiry forms
- Routes all negotiations to the Negotiation Room escrow-style layout described
- Fixes any missing sections not showing in modals (scroll/conditional render)

After implementation, provide:
- short list of files changed
- how to test each lane (Wholesale, Capital, Listings) across Grid/Swipe/Detail entry points
