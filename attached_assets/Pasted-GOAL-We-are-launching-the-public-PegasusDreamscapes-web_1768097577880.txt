GOAL:
We are launching the public PegasusDreamscapes website NOW, while continuing to build MarketFlow in a safe “Beta” mode without breaking production. We need a clean Production + Dev workflow (like real SaaS) so the front site stays stable and MarketFlow can evolve quickly.

NON-NEGOTIABLES:
1) DO NOT duplicate the entire site into random copies.
2) We must have:
   - PRODUCTION (live, stable, public)
   - DEVELOPMENT / NEXT (private or separate URL for ongoing work)
3) MarketFlow is allowed to be visible publicly, but it MUST be labeled and gated as “Beta / Under Construction” until features are complete.
4) All “offer” interactions must be consistent across views (Discovery cards vs Deal page vs Offer Studio). No duplicate forms with different fields for the same action.

PHASE 1 — GO LIVE SAFELY (PRODUCTION)
A) Production site should include:
   - Landing / Home (Designed Profits. Elevated Communities.)
   - Services / Sell / Buy / Invest / Dreamscaper / Contact / About/Story sections
   - “Explore” mega menu (as shown in screenshots)
   - Basic lead capture forms: “Sell Your Property”, “Partner With Us / Invest”
B) MarketFlow on Production must be:
   - Accessible from top nav “MarketFlow”
   - Clearly labeled with a banner + badge:
     - “MarketFlow Beta — Under Construction”
     - Short line: “Some features may be limited while we finalize v1.”
   - Add a “What’s available now” section on the MarketFlow landing:
     - Browse deals (Wholesale / Capital Raises / Listings)
     - View details
     - Save deals
     - Basic offer actions (Accept Terms / Counter)
   - Add a “Coming next” section:
     - Offer Studio improvements
     - Capital raise forms + negotiation enhancements
     - Document uploads / underwriting / etc.

PHASE 2 — SET UP DEV / NEXT WORKFLOW (NO COPYING)
Implement a proper workflow so we can build features without breaking production:

1) Create a “dev/next” deployment environment:
   - Use Git branching (main = production, dev = next)
   - OR if Replit deploys differently: maintain two deployments:
     - Production URL = main branch
     - Staging/Dev URL = dev branch
2) Add environment variables:
   - NEXT_PUBLIC_APP_ENV=production|staging
   - Use it to control Beta banners, debug tools, and feature flags
3) Staging/dev can share the same database OR use a staging DB.
   - If sharing DB: clearly separate test data (add a “is_test” flag).
   - Prefer staging DB if possible, but do not block launch if not.

PHASE 3 — MARKETFLOW BETA STRUCTURE + GATING
We need a clean “Beta” experience (professional and intentional, not broken).

A) Add a global Beta badge + banner for MarketFlow routes:
   - Routes: /marketflow/* (or current equivalent)
   - Banner options:
     - Top sticky banner, dismissible
     - Beta chip next to MarketFlow nav button
B) Feature gating:
   - For incomplete features, show “Coming Soon” UI instead of broken UI.
   - Add a simple feature flag helper:
     - const flags = { negotiationRoomV2: false, capitalRaiseFormV2: false, ... }
     - Use flags to hide/disable unfinished flows on Production.
   - On DEV, flags can be enabled for testing.

PHASE 4 — UNIFY ALL “OFFER” FORMS (VERY IMPORTANT)
Right now there are multiple offer forms that look/behave differently depending where you click (Discovery vs Deal page vs Offer Studio). This must be unified.

RULE:
Same action = same underlying component + same validation + same payload.

A) Standardize the entry actions across all lanes:
   1) “Accept Terms” (1-click commit to poster terms)
   2) “Counter Offer” (opens Offer Studio or a unified modal that then goes Offer Studio)
B) Remove/replace duplicate forms so there is ONE source of truth:
   - Create shared components:
     - <AcceptTermsModal />
     - <CounterOfferModal /> (or route to Offer Studio)
     - <OfferStudioWizard />
C) Offer Studio Flow must start with:
   Step 0: Accept vs Counter (if user chooses Accept, it submits immediately)
   If Counter:
     Step 1: Deal Structure (Cash / Debt / Equity / Hybrid)
     Step 2: Terms & Price (price, assignment fee, EMD, close date, inspection, contingencies)
     Step 3: Financing (funding source, proof of funds, lender info if needed)
     Step 4: Review & Submit
D) After submission:
   - It creates/updates a negotiation thread
   - Redirect to Negotiation Room

PHASE 5 — NEGOTIATION ROOM VISION (ESCROW-LIKE)
Negotiation Room should open ONLY when there’s a negotiation initiated between two principals (poster/operator and buyer/investor). Think of it like escrow + chat + offer ladder.

Negotiation Room layout (3-column):
1) Left/Main: Offer Ladder / History (log of all offers/counters with timestamps, who sent it, status)
2) Middle/Side: Chat (real-time conversation between both parties)
3) Right: Peggy AI Assistant panel (deal guidance + quick actions + risk flags)

Tabs at top are ok:
- Offer Ladder
- Chat
- Terms (summary)

FUNCTIONS:
- Accept / Reject / Quick Counter buttons on the most recent offer
- Always show deal summary card (address, photo, key numbers)
- “Pending” states are crystal clear

PHASE 6 — CAPITAL RAISE FORMS (NEEDED)
We still need the capital raise offer/counter forms to be structured and professional.

Implement a unified Capital Raise “Investment Terms” form:
- Investment Amount
- Structure: Debt vs Equity vs Hybrid
- Debt fields: Interest %, term length, payment frequency, lien position
- Equity fields: preferred return %, profit split %, hold period, waterfall notes
- Hybrid fields: combined
- Risk acknowledgment checkbox + accreditation toggle (optional)
- Message to operator
This must match design style of the rest (same UI kit, spacing, headers, validation).

PHASE 7 — FRONT WEBSITE COPY ENHANCEMENTS (LIGHT EDIT)
Make small improvements to the wording while keeping the same structure:
- Clarify what Pegasus does in 1–2 lines under hero
- Make CTAs clearer (Sell / Partner / Explore)
- Add a “How it works” section that matches MarketFlow concept
Do NOT rewrite everything; just tighten and make more premium/credible.

DELIVERABLES / ACCEPTANCE CRITERIA:
1) Public site can be deployed today with clean navigation and lead capture.
2) MarketFlow is visible but clearly labeled Beta with gating so nothing looks broken.
3) A dev/next environment exists for ongoing work.
4) Offer forms are unified (same components/fields/payload) across discovery, deal page, and offer studio.
5) Negotiation Room is escrow-like with offer ladder + chat + Peggy panel.
6) Capital Raise forms are built and consistent with the rest of the UI.

Please confirm back with:
- The exact URLs for production + staging/dev
- Which feature flags are enabled on prod vs dev
- A checklist showing what was completed and what remains.
