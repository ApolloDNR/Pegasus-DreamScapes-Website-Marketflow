You are working on the **Pegasus DreamScapes WEBSITE + PEGASUS MARKETPLACE** app.

IMPORTANT CONTEXT
- This Replit project is ONLY:
  - The public-facing Pegasus website (marketing, credibility, funnels)
  - The Pegasus Marketplace (where wholesalers, dreamscapers, investors, and buyers log in, post/browse deals, and have dashboards)
- Pegasus HQ (internal OS / command center) is a SEPARATE app that already uses the SAME Supabase backend.
- Do NOT try to rebuild HQ here. This project should focus on:
  - Website pages
  - Marketplace dashboards
  - Role-based auth and user experience
  - Forms and flows that write to Supabase tables which HQ can later read.

I want you to:
1) Confirm Supabase is properly connected and reused here.
2) Set up roles, routes, and core data structures for the WEBSITE + MARKETPLACE.
3) Implement the main public funnels (Sell, Invest, Buy, Submit Deal).
4) Implement role-based dashboards for Marketplace users.
5) Scaffold external APIs (Google Maps/Places, SendGrid email, Supabase Storage) in a safe way that doesn’t break if keys are missing.
6) Keep the existing visual design and layout style as much as possible, but reorganize structure and flows to match this blueprint.


==================================================
PHASE 1 – CONFIRM SUPABASE SETUP (REUSE EXISTING)
==================================================

1. Confirm Supabase env vars exist:
   - SUPABASE_URL
   - SUPABASE_ANON_KEY

   If they do not exist, clearly tell me how to add them in Replit (Secrets) and pause any Supabase-dependent changes until they are present.

2. Confirm there is a single Supabase client (for example: src/lib/supabaseClient.ts or similar) and that the whole app uses that client:

   - import { createClient } from '@supabase/supabase-js'
   - const supabaseUrl = process.env.SUPABASE_URL
   - const supabaseAnonKey = process.env.SUPABASE_ANON_KEY
   - export const supabase = createClient(supabaseUrl, supabaseAnonKey)

3. Add a basic connection test (dev-only) to ensure Supabase is reachable:
   - For example, call supabase.auth.getSession() and log the result, or select from a simple table.

Do NOT move forward until Supabase connectivity is confirmed.

==================================================
PHASE 2 – ROLES & USER PROFILES FOR MARKETPLACE
==================================================

This WEBSITE + MARKETPLACE app uses Supabase Auth with the following roles:

- admin
- pegasus_wholesaler  (internal)
- wholesaler          (external)
- pegasus_dreamscaper (internal)
- dreamscaper         (external flipper/developer)
- investor
- buyer_retail        (retail buyers of finished Pegasus homes)
- buyer_investment    (buyers of wholesale / investment deals)

Use or create a user_profiles table with at least:

- user_id          (references auth.users.id)
- primary_role     (one of the roles above)
- display_name
- company_name
- location
- avatar_url
- bio
- is_pegasus_badged (boolean)
- pegasus_role_type  (optional detail like "Pegasus Wholesaler", "Pegasus Dreamscaper")

Also create:

user_badges:
- id
- user_id
- badge_type  (e.g. pegasus_wholesaler, pegasus_dreamscaper, verified_investor)
- label
- description
- created_at

user_reputation:
- user_id
- trust_score      (0–100 or 0–1000)
- rating           (1–5)
- deals_closed_count
- on_time_closings_count
- cancellations_count
- last_updated_at

Sign-up / onboarding:
- When a user signs up, they must pick a primary role (wholesaler, dreamscaper, investor, buyer, etc.).
- Store this in user_profiles.primary_role.
- This will determine which Marketplace dashboard they see.

==================================================
PHASE 3 – ROUTES AND NAVIGATION STRUCTURE
==================================================

We need a clean distinction between public routes (no login) and Marketplace (login required).

PUBLIC ROUTES (no login required)
- /            → Home
- /sell        → Sell Your Property
- /invest      → Invest With Pegasus
- /buy         → Buy a Pegasus Home
- /submit-deal → Submit a Wholesale Deal
- /about       → About Pegasus Dreamscapes
- /login       → Login / Signup
- /marketplace → Marketplace teaser / explainer (public landing for the Marketplace)

MARKETPLACE ROUTES (require login)
- /dashboard              → role-based redirect:
  - wholesaler, pegasus_wholesaler      → /marketplace/wholesaler
  - dreamscaper, pegasus_dreamscaper    → /marketplace/dreamscaper
  - investor                            → /marketplace/investor
  - buyer_retail, buyer_investment      → /marketplace/buyer
  - admin                               → /marketplace/admin (basic admin tools for marketplace)

- /marketplace/wholesaler
- /marketplace/dreamscaper
- /marketplace/investor
- /marketplace/buyer
- /marketplace/admin

AUTH GUARDS:
- If a user is not logged in and goes to /dashboard or /marketplace/*, redirect to /login.
- If a logged-in user tries to access a marketplace route that doesn’t match their role, redirect them to their correct dashboard.

NAVIGATION:
- Public navbar: Logo, Home, Sell, Invest, Buy, Submit a Deal, About, Login/Sign Up
- Logged-in navbar: same core links, but replace Login with a profile/avatar menu including: Dashboard, Profile, Logout.
- Within /marketplace/*, use a consistent dashboard layout (sidebar or similar) with role-specific nav items.

==================================================
PHASE 4 – PUBLIC FUNNELS (FORMS & FLOWS)
==================================================

Create or confirm the following Supabase tables (if not already created by HQ work):

seller_leads:
- id
- name
- phone
- email
- address
- property_type
- situation
- best_time_to_contact
- notes
- source        (default "Website - Seller")
- status        (New, Contacted, Under Review, etc.)
- created_at

wholesale_deals (basic for website side; HQ can extend it):
- id
- wholesaler_id   (user_id from auth)
- address
- arv_estimate
- asking_price
- repair_estimate
- assignment_fee
- photo_urls      (array or JSON)
- occupancy_status
- close_timeline
- notes
- status          (Under Review, Approved, Rejected, Listed)
- is_public       (boolean, default false initially)
- created_at

saved_items (used later for buyers/investors to save deals):
- id
- user_id
- entity_type   (e.g. 'deal', 'listing')
- entity_id
- created_at

4.1 /sell – Seller Funnel
- Build/keep a page explaining how selling to Pegasus works.
- Include a form with:
  - Name
  - Phone
  - Email
  - Property address
  - Property type
  - Situation/reason
  - Best time to contact
  - Notes
- On submit, insert a new row into seller_leads with:
  - source = 'Website - Seller'
  - status = 'New'
- After submission, show a clean success/thank-you state. No login required.

4.2 /invest – Investor Funnel
- Explain:
  - How investors participate (debt, equity, JV, etc.).
  - Example returns (without promising).
  - Role of Pegasus Dreamscapers and the marketplace.
- CTAs:
  - "Create Investor Account" (prefill investor role on signup).
  - "Log in as Investor" (if already signed up).
- When signup comes from /invest, set primary_role = investor unless overridden.

4.3 /buy – Buyer Funnel
- Explain that there are two buyer types:
  - Retail buyers of finished Pegasus homes.
  - Investment/wholesale buyers looking for deals.
- Provide two clear CTAs:
  - "Buy a Finished Pegasus Home" → flow where buyer is or becomes buyer_retail.
  - "Buy Investment / Wholesale Deals" → flow where buyer is or becomes buyer_investment.
- Each CTA should route user into login/signup flow with the appropriate primary_role selected by default.

4.4 /submit-deal – Wholesaler Funnel
- Public (not logged-in):
  - Page explains how wholesalers can submit deals to Pegasus via Marketplace.
  - Explain difference between Pegasus Wholesalers (internal, badged) and external wholesalers.
  - CTA: "Sign Up / Log In as Wholesaler to Submit Your Deals".
- Logged-in (wholesaler or pegasus_wholesaler):
  - Show a deal submission form with fields:
    - Address
    - ARV estimate
    - Asking price
    - Repair estimate
    - Contract/assignment fee
    - Photo URLs or file uploads (for now, at least URLs; Storage can come later)
    - Occupancy status
    - Close timeline
    - Notes
  - On submit, create a row in wholesale_deals with:
    - wholesaler_id = current user
    - status = 'Under Review'
    - is_public = false

==================================================
PHASE 5 – MARKETPLACE ROLE DASHBOARDS
==================================================

Create dashboards that present a “mini-office” for each type of user.

Use the same general layout/components, but change what data is shown based on role.

5.1 Wholesaler Dashboard – /marketplace/wholesaler
- Summary metrics:
  - Total Submitted Deals
  - Under Review
  - Approved/Listed
- Table or card list of deals from wholesale_deals where wholesaler_id = current user.
  - Show: address, status, created_at, basic numbers (ARV, asking, repair).
- Actions:
  - "Submit New Deal" → /submit-deal
  - "View Deal Details" (simple details page is enough for now).
- Reserve a section for “Dreamscaper Interest” / JV Requests (can be placeholders for now).

5.2 Dreamscaper Dashboard – /marketplace/dreamscaper
- Section: "Browse Wholesale Deals"
  - Show deals from wholesale_deals that are approved / is_public = true.
  - Include:
    - Address
    - ARV, asking, repair
    - Wholesaler info (display_name from user_profiles)
    - Badge if wholesaler is pegasus_wholesaler or is_pegasus_badged
    - Basic trust/reputation info from user_reputation/user_badges.
    - CTA: "View Deal".
- Section: "My Deals / Projects"
  - Placeholder list of deals that the dreamscaper has flagged or claimed.
  - For now, just design the UI and stub out the data; HQ will handle deep project logic.

5.3 Investor Dashboard – /marketplace/investor
- Top summary:
  - Active Commitments
  - Total Capital Indicated/Committed
  - Active Projects
  - Completed Investments (even if just placeholder for now)
- Section: "Available Deals"
  - Use wholesale_deals (or later capital_raises table) to show deals that are raising capital.
  - Show:
    - Project/deal name
    - Dreamscaper info and badges
    - Basic terms (debt vs equity, target returns, timeline)
    - CTA: "View Deal" / "Express Interest" (record interest in a placeholder table or just design UI).
- Section: "My Commitments"
  - Deals the investor has expressed interest in (placeholder list).
- Section: "Past Investments"
  - Placeholder list of completed deals for later.

5.4 Buyer Dashboard – /marketplace/buyer
- Distinguish behavior based on primary_role:
  - If buyer_retail:
    - Saved homes
    - Active inquiries/tours (placeholder)
    - Offers submitted (placeholder)
  - If buyer_investment:
    - Saved investment deals
    - LOIs / offers (placeholder)
    - Closed deals (placeholder)
- Use saved_items table for saved homes/deals.

5.5 Admin Marketplace Dashboard – /marketplace/admin
- Simple admin tools for WEBSITE + MARKETPLACE (NOT HQ-level admin):
  - View all users and their roles.
  - View all wholesale_deals, seller_leads.
  - Approve or reject deals (set status and is_public).
  - Basic moderation of profiles and badges (toggle is_pegasus_badged, assign user_badges entries).

==================================================
PHASE 6 – PROFILE, BADGES, REPUTATION DISPLAY
==================================================

Create a dedicated Profile page/component:

- Shows:
  - Avatar
  - Display name
  - Primary role
  - Company name
  - Location
  - Bio
- Shows badges (from user_badges):
  - For example: Pegasus Dreamscaper, Pegasus Wholesaler, Verified Investor.
- Shows reputation (from user_reputation):
  - Rating (e.g. 4.8 ★)
  - Trust score
  - Deals closed

Use small badge chips/icons on relevant places in the UI:
- On deal cards, show if wholesaler is Pegasus-badged.
- On capital-raising deals, show if dreamscaper is Pegasus-badged or high-trust.

==================================================
PHASE 7 – EXTERNAL APIS (SCAFFOLD ONLY, SAFE)
==================================================

Set up scaffolding for these APIs in a way that:
- Uses environment variables.
- Does NOT crash if keys are missing.
- Has clear comments where to expand later.

1) Google Maps / Places
- Purpose:
  - Address autocomplete on /sell and /submit-deal.
  - Later, map previews for deals.
- Env var:
  - GOOGLE_MAPS_API_KEY
- Implementation:
  - Create a utility module for Places autocomplete.
  - Use it in address input components if the key exists.
  - If the key is missing, fall back to a plain text input.

2) Email Provider (SendGrid or similar)
- Purpose:
  - Send confirmation emails for seller_leads.
  - Send notifications when a wholesaler submits a new deal.
- Env var:
  - SENDGRID_API_KEY (or similar).
- Implementation:
  - Create an email service module.
  - For now, either:
    - Log the email payload if SENDGRID_API_KEY is missing, OR
    - Set up minimal sending logic if key exists.
  - Do not make the UI depend on the email succeeding.

3) Supabase Storage
- Purpose:
  - Store property photos and documents.
- Implementation:
  - Create basic upload utilities and call Supabase Storage APIs.
  - Use a bucket like “deal-photos” or similar.
  - If storage is not fully configured yet, allow users to enter plain URLs and design the UI for future enhancement.

4) Optional (Future):
- Twilio for SMS alerts.
- DocuSign / Dropbox Sign for contracts.
- Stripe or another payment processor for transaction/fee flows.
For now, only add TODO comments and potential env var names; do not implement these fully yet.

==================================================
PHASE 8 – WHAT TO PRESERVE AND WHAT TO REFACTOR
==================================================

- Preserve as much of the CURRENT visual design (layout, colors, components) as possible.
- Refactor the routing, flows, and database interactions to align with:
  - The role system.
  - The public funnels.
  - The marketplace dashboards.

DO NOT:
- Implement HQ logic or command centers in this project.
- Implement full CMS editing of website text yet (content can stay static for now).
- Break existing working components without providing equivalent or better replacements.

==================================================
FINAL SUMMARY (WHAT I EXPECT BACK FROM YOU)
==================================================

After implementing this blueprint, I want you to provide a summary of:

1) Supabase configuration:
   - Which client file you are using, and how.
   - Confirmation that this app uses the same Supabase project as HQ.

2) Tables used by this WEBSITE + MARKETPLACE app:
   - user_profiles, user_badges, user_reputation
   - seller_leads
   - wholesale_deals
   - saved_items
   - any others you introduced

3) Route map:
   - All public routes and what they do.
   - All /marketplace/* routes and what they do.

4) Role-based behavior:
   - How roles are assigned at signup.
   - How /dashboard redirects by role.
   - How each dashboard is scoped to the logged-in user.

5) External APIs:
   - Which ones are scaffolded (Google Maps/Places, email, storage).
   - Which env vars you expect (GOOGLE_MAPS_API_KEY, SENDGRID_API_KEY, etc.).

6) Placeholders vs finished:
   - What is fully wired end-to-end.
   - What is UI-only / placeholder for future phases (e.g., capital-raise details, investor commitments, etc.).

Please implement this cleanly and modularly so I can expand on top of it with HQ later.